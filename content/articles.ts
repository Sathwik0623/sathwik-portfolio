import { Article } from "./types";

// No articles published yet. Add entries here as you write them —
// each one automatically appears in the /articles listing and gets its own page.
export const articles: Article[] = [];
